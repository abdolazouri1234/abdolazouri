<Window x:Class="SupermarketDesktop.Views.PurchasePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SupermarketDesktop.ViewModels"
        mc:Ignorable="d"
        Title="Supermarket Purchase Console"
        Height="760"
        Width="1220"
        MinHeight="680"
        MinWidth="1080"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6FB">
    <Window.DataContext>
        <vm:PurchasePageViewModel />
    </Window.DataContext>

    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*" />
            <ColumnDefinition Width="18" />
            <ColumnDefinition Width="1.4*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" CornerRadius="16" Background="White" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel LastChildFill="False">
                    <TextBlock DockPanel.Dock="Left"
                               Text="Product Catalog"
                               FontSize="24"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                    <StackPanel DockPanel.Dock="Right"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <TextBox Width="260"
                                 Height="34"
                                 VerticalContentAlignment="Center"
                                 Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Content="Search"
                                Command="{Binding SearchCommand}"
                                Margin="8,0,0,0"
                                Width="86"
                                Height="34"
                                Background="#4660F3"
                                Foreground="White" />
                    </StackPanel>
                </DockPanel>

                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          RowHeaderWidth="0"
                          HeadersVisibility="Column"
                          AutoGenerateColumns="False"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="1.1*" />
                        <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="2*" />
                        <DataGridTextColumn Header="Stock" Binding="{Binding AvailableQuantity}" Width="0.8*" />
                        <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C2}" Width="1*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Column="2" CornerRadius="16" Background="White" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="12" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Current Purchase"
                           FontSize="24"
                           FontWeight="SemiBold" />

                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Button Content="Add Selected Item"
                            Command="{Binding AddToCartCommand}"
                            Width="150"
                            Height="34"
                            Background="#07AD74"
                            Foreground="White" />
                    <Button Content="Remove"
                            Command="{Binding RemoveFromCartCommand}"
                            Width="92"
                            Height="34"
                            Margin="10,0,0,0"
                            Background="#E95454"
                            Foreground="White" />
                </StackPanel>

                <DataGrid Grid.Row="4"
                          ItemsSource="{Binding CartItems}"
                          SelectedItem="{Binding SelectedCartItem}"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          FontSize="14">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="2*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="0.7*" />
                        <DataGridTextColumn Header="Price" Binding="{Binding UnitPrice, StringFormat=C2}" Width="1*" IsReadOnly="True" />
                        <DataGridTextColumn Header="Total" Binding="{Binding LineTotal, StringFormat=C2}" Width="1*" IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="6" Padding="12" Background="#F6F8FF" CornerRadius="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Discount %:" Width="95" VerticalAlignment="Center" />
                                <TextBox Text="{Binding DiscountRate, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"
                                         Width="70"
                                         VerticalContentAlignment="Center" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Tax %:" Width="95" VerticalAlignment="Center" />
                                <TextBox Text="{Binding TaxRate, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"
                                         Width="70"
                                         VerticalContentAlignment="Center" />
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding Summary.Subtotal, StringFormat=Subtotal: {0:C2}}" FontSize="15" />
                            <TextBlock Text="{Binding Summary.Discount, StringFormat=Discount: -{0:C2}}" FontSize="15" Margin="0,4,0,0" />
                            <TextBlock Text="{Binding Summary.Tax, StringFormat=Tax: +{0:C2}}" FontSize="15" Margin="0,4,0,0" />
                            <TextBlock Text="{Binding Summary.Total, StringFormat=Grand Total: {0:C2}}"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Margin="0,8,0,0" />
                            <Button Content="Checkout"
                                    Command="{Binding CheckoutCommand}"
                                    Margin="0,12,0,0"
                                    Height="40"
                                    Width="140"
                                    Background="#4660F3"
                                    Foreground="White" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
